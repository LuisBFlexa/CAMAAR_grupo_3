# Módulo ApplicationCable
module ApplicationCable
  # Esta classe define um canal base para o Action Cable.
  class Channel < ActionCable::Channel::Base
    # Esta é uma classe vazia que herda de ActionCable::Channel::Base
    # e serve como a classe base para todos os canais em um aplicativo Rails.
  end
end
